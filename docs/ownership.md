# Parallel work ownership

Root: scaffold, contracts, transport, DevTools shell, content picker integration, fixtures, release checks.

Resolver agent: `src/core/resolve`, `src/adapters/capture-live.ts`, `src/shared/eval-fns.ts`, resolver tests.

Editing agent: `src/core/selector`, `src/core/emit`, `src/content/override`, editing/export tests.

UI agent: `src/panel`, `src/sidebar`, `src/popup`, `src/ui`, `src/core/color`, UI/color tests.

Spike agent: `docs/spikes.md`, `test/spikes`. Audit and hardening begin after integration.
